/**
 * @file Main entry point for eslint-config-whippinai
 *
 * This file exports all available configurations.
 * Users can import the default base config or specific configs:
 *
 * - Base JavaScript: require('eslint-config-whippinai')
 * - TypeScript: require('eslint-config-whippinai/typescript')
 * - React (with TypeScript): require('eslint-config-whippinai/react')
 */

module.exports = require('./base')
